// ----------------------------------------- CONFIGURE AJAX -------------------------------------------	
	//jQuery(document).ajaxStart(ajaxStart);
	//jQuery(document).ajaxComplete(ajaxComplete);
	//jQuery(document).ajaxSuccess(ajaxSuccess);

		
// ----------------------------------------- LOADING IMAGES -------------------------------------------
	//$j('a', 'input:submit').click(function(){ showLoadingImage(); });
	//$j('a.a-json').aJson();
