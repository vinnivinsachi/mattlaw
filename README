TO DEPLOY:
1) clone submodules (library | public/nwPublicLibrary)
	run these commands from your project root directory:
		rm -r public/nwPublicLibrary
		git clone git@github.com:NovumWare/NovumWarePublicLibrary.git public/nwPublicLibrary
		rm -r library
		git clone git@github.com:NovumWare/Zend_Library.git library
2) create MySQL DB for the project
3) copy file and update DB settings in the newly copied file (application/configs/local_settings.ini)
	run these commands from the project root directory
		cp SETUP/local_settings.ini application/configs/local_settings.ini
4) create log file with write permission at logs/log.txt
	run these commands from the project root directory:
		mkdir logs
		chmod -R 777 logs
		touch logs/log.txt
5) create Smarty temp folder with write permission at tmp/templates_c
	run these commands from the project root directory:
		mkdir tmp
		mkdir tmp/templates_c
		chmod -R 777 tmp/templates_c
6) copy file and update variable @siteRoot in the newly copied file (public/stylesheets/less/local_variables.less)
	run these commands from the project root directory
		cp SETUP/local_variables.less public/stylesheets/less/local_variables.less
7) define $_sessionNamespace in the file: application/Bootstrap.php